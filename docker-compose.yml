version: '3'

services:
  jupyter:
    #cpuset: "0-6" # only enable if multicore
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
